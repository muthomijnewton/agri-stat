# Web frontend environment variables
# Vite only exposes variables with the VITE_ prefix to the browser bundle.
# Copy this file to .env and set the URL for your backend.
VITE_API_URL=http://localhost:8000/api
